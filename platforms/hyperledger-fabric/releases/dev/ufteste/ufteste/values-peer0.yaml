apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ufteste-peer0
  namespace: ufteste-net
  annotations:
    fluxcd.io/automated: "false"
spec:
  interval: 1m
  releaseName: ufteste-peer0
  chart:
    spec:
      interval: 1m
      sourceRef:
        kind: GitRepository
        name: flux-local
        namespace: flux-local
      chart: platforms/hyperledger-fabric/charts/fabric-peernode    
  values:
    metadata:
      namespace: ufteste-net
      images:
        couchdb:  ghcr.io/hyperledger/bevel-fabric-couchdb:2.2.2
        peer:  ghcr.io/hyperledger/bevel-fabric-peer:2.2.2
        alpineutils: ghcr.io/hyperledger/bevel-alpine:latest

    annotations:  
      service:
        - example1: example2
      pvc:
      deployment:
        
    peer:
      name: peer0
      gossippeeraddress: peer0.ufteste-net:7051
      gossipexternalendpoint: peer0.ufteste-net:7051
      localmspid: uftesteMSP
      loglevel: info
      tlsstatus: true
      builder: hyperledger/fabric-ccenv:2.2.2
      couchdb:
        username: ufteste-user

    storage:
      peer:
        storageclassname: ufteste-bevel-storageclass
        storagesize: 512Mi
      couchdb:
        storageclassname: ufteste-bevel-storageclass
        storagesize: 1Gi

    vault:
      role: vault-role
      address: http://10.0.2.15:8200
      authpath: localufteste
      secretprefix: secretsv2/data/ufteste/peerOrganizations/ufteste-net/peers/peer0.ufteste-net
      serviceaccountname: vault-auth
      type: hashicorp
      imagesecretname: ""
      secretcouchdbpass: secretsv2/data/ufteste/credentials/ufteste-net/couchdb/ufteste?user

    service:
      servicetype: ClusterIP
      ports:
        grpc:
          clusteripport: 7051
        events:
          clusteripport: 7053
        couchdb:
          clusteripport: 5984
        metrics: 
          enabled: False
          clusteripport: 9443     
    proxy:
      provider: "none"
      external_url_suffix: develop.local.com

    config:
      pod:
        resources:
          limits:
            memory: 512M
            cpu: 1
          requests:
            memory: 512M
            cpu: 0.25
